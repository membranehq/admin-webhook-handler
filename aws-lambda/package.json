{
  "name": "membrane-aws-lambda",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && sam local start-api",
    "deploy": "npm run build && sam deploy",
    "test": "npm run build && sam local invoke MembraneWebhookFunction -e events/test-event.json"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.10.0",
    "@membranehq/sdk": "^0.5.1",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3"
  }
}